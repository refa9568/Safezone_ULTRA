// Copy this file to cloud_tts_config.dart (same folder) and paste your real
// key. cloud_tts_config.dart is gitignored so the real key never gets
// committed.
//
// Get a key: Google Cloud Console -> APIs & Services -> Credentials, on a
// project with the "Cloud Text-to-Speech API" enabled (the Firebase project
// this app already uses works fine). Restrict the key to that API.
const String cloudTtsApiKey = 'PUT_YOUR_CLOUD_TTS_API_KEY_HERE';
